import React from 'react';import { checkProd } from '~/util';import { IStartEndDate } from 'marketing/api/marketingHandler';const localDashBoard = `link-admin${checkProd ? '' : '-dev'}-dashboard`;export function fireDashboardLocal(): void {  localStorage.removeItem(localDashBoard);}const initialDate = {  start_date: new Date(2022, 4, 27, 0, 0, 0),  end_date: new Date(),};export function useDashboardLocal(): [  IStartEndDate,  (data: IStartEndDate) => void,  React.Dispatch<React.SetStateAction<IStartEndDate>>,] {  const [check] = React.useState(localStorage.getItem(localDashBoard));  const [dashboard, setDashboard] = React.useState<IStartEndDate>(    check ? JSON.parse(check) : initialDate,  );  const settingLocal = React.useCallback((data: IStartEndDate) => {    setDashboard(data);    localStorage.setItem(localDashBoard, JSON.stringify(data));  }, []);  return [dashboard, settingLocal, setDashboard];}// export function useLocalDashboard(): [//   IStartEndDate,//   (data: IStartEndDate) => void,// ] {//   const [check] = React.useState(localStorage.getItem(localDashBoard));////   const dashboard = React.useMemo<IStartEndDate>(() => {//     if (check) {//       return JSON.parse(check);//     } else {//       return initialDate;//     }//   }, [check]);////   const settingLocal = React.useCallback((data: IStartEndDate) => {//     localStorage.setItem(localDashBoard, JSON.stringify(data));//   }, []);////   return [dashboard, settingLocal];// }